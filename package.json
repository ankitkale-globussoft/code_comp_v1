{
  "name": "code-arena",
  "version": "1.0.0",
  "description": "CodeArena multiplayer coding game",
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend && npm run generate --prefix backend",
    "start": "npm start --prefix backend"
  }
}
